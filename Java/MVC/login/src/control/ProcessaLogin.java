package control;

import java.util.ArrayList;

import model.Login;

public class ProcessaLogin {

	private ArrayList<Login > user = new ArrayList<>();
	
	public void login(String usuario, String senha) {
		
		if(usuario == user.usuario() && senha == user.senha() {
			
		}
	}
}
